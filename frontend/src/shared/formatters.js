export const formatMoney = (value) => Number(value || 0).toLocaleString('en-IN', { style: 'currency', currency: 'INR', maximumFractionDigits: 0 });
export const formatArea = (value) => `${Number(value || 0).toLocaleString('en-IN')} sq ft`;